service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read;
    }
    match /rounds {
      allow write;
    }
    match /highscores {
      allow write;
    }
  }
}